<div align="center">
  <h1>Product Shelf</h1>
  <p>SPA-витрина на React + TypeScript + Vite с Zustand и React Router</p>
  <p>Создано mircocosov ❤️</p>
</div>

## 🚀 Возможности

- Каталог продуктов с фильтрацией по избранному, категориям, поиску и выбором размера страницы
- Карточки с лайком, удалением и быстрым переходом на подробную страницу
- Детальная страница с галереей, редактированием и удалением
- Создание и редактирование продуктов через единую форму с валидацией и загрузкой изображений (URL или файл)
- Глобальный Zustand-store со всеми действиями (fetch, like, delete, create, update)
- Актуализация UI: плавная пагинация с прокруткой вверх, строгий тёмный дизайн «бутика»

## 🧱 Стек

- React + TypeScript + Vite
- Zustand для состояния
- React Router v7
- CSS без библиотек (кастомная премиум-тема)

## 📦 Скрипты

```bash
npm install      # установка зависимостей
npm run dev      # разработка + HMR
npm run build    # production-сборка
npm run preview  # предпросмотр собранного приложения
```

## 🗂️ Архитектура

```
src/
├─ components/    # переиспользуемые компоненты (Layout, карточки, формы)
├─ pages/         # products, detail, create, edit
├─ store/         # Zustand store c API и CRUD-экшенами
├─ types/         # типы доменной модели
├─ index.css      # глобальный строгий стиль приложения
└─ App.tsx        # маршруты + общая структура
```

## ✨ Дополнительно

- Плавные hover/focus-состояния для всех интерактивных элементов
- Карточки с одинаковой высотой описаний за счёт `line-clamp`
- Файловый инпут скрыт, но даёт превью загруженного изображения

---

> С любовью к премиальным интерфейсам от mircocosov ❤️
